import{describe,expect,it}from'vitest';import{repurpose,type Source}from'./repurpose';const source:Source={title:'Stop automating chaos',body:'Automation amplifies the process underneath it. Map the workflow before choosing tools. A clear owner and success measure prevent expensive rework.',audience:'operations leaders',tone:'Direct',cta:'Which workflow would you map first?'};describe('repurpose',()=>{it('creates four channel drafts',()=>expect(repurpose(source)).toHaveLength(4));it('keeps the chosen call to action',()=>expect(repurpose(source)[0].content).toContain(source.cta));});
